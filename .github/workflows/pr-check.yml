name: pull-request-check
on:
  pull_request:
    branches:
      - main
jobs:
    pr-check:
      runs-on: ubuntu-latest
      steps:
        - name: Check branch
          run: echo "PR check running for branch :" ${{ github.head_ref}} 
